name: Release PDF

on:
  push:
    branches: [main]

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: typst-community/setup-typst@v4

      - name: Build PDF
        run: |
          mkdir -p dist
          typst compile --root . --font-path pdf/fonts pdf/cv.typ dist/cv.pdf

      - name: Upload to rolling release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest CV"
          body: "Auto-generated from commit ${{ github.sha }} on ${{ github.event.head_commit.timestamp }}"
          files: dist/cv.pdf
          make_latest: "true"
          overwrite: true
